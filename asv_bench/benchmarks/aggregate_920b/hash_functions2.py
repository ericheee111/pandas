# Generated by generate_aggregate_benchmarks.py; do not edit.


"""Self-contained 920b aggregate benchmarks for hash_functions."""


# ---- Inlined official source: hash_functions ----
import numpy as np
import pandas as pd

class _Official_hash_functions_UniqueForLargePyObjectInts:

    def setup(self):
        lst = [x << 32 for x in range(5000)]
        self.arr = np.array(lst, dtype=np.object_)

    def time_unique(self):
        pd.unique(self.arr)

class _Official_hash_functions_Float64GroupIndex:

    def setup(self):
        self.df = pd.date_range(start='1/1/2018', end='1/2/2018', periods=10 ** 6).to_frame()
        self.group_index = np.round(self.df.index.astype(int) / 10 ** 9)

    def time_groupby(self):
        self.df.groupby(self.group_index).last()

class _Official_hash_functions_UniqueAndFactorizeArange:
    params = range(4, 16)
    param_names = ['exponent']

    def setup(self, exponent):
        a = np.arange(10 ** 4, dtype='float64')
        self.a2 = (a + 10 ** exponent).repeat(100)

    def time_factorize(self, exponent):
        pd.factorize(self.a2)

    def time_unique(self, exponent):
        pd.unique(self.a2)

class _Official_hash_functions_Unique:
    params = ['Int64', 'Float64']
    param_names = ['dtype']

    def setup(self, dtype):
        self.ser = pd.Series([1, pd.NA, 2, *range(100000)] * 3, dtype=dtype)
        self.ser_unique = pd.Series([*range(300000), pd.NA], dtype=dtype)

    def time_unique_with_duplicates(self, exponent):
        pd.unique(self.ser)

    def time_unique(self, exponent):
        pd.unique(self.ser_unique)

class _Official_hash_functions_NumericSeriesIndexing:
    params = [(np.int64, np.uint64, np.float64), (10 ** 4, 10 ** 5, 5 * 10 ** 5, 10 ** 6, 5 * 10 ** 6)]
    param_names = ['dtype', 'N']

    def setup(self, dtype, N):
        vals = np.array([*range(55), 54, *range(55, N - 1)], dtype=dtype)
        indices = pd.Index(vals)
        self.data = pd.Series(np.arange(N), index=indices)

    def time_loc_slice(self, index, N):
        self.data.loc[:800]

class _Official_hash_functions_NumericSeriesIndexingShuffled:
    params = [(np.int64, np.uint64, np.float64), (10 ** 4, 10 ** 5, 5 * 10 ** 5, 10 ** 6, 5 * 10 ** 6)]
    param_names = ['dtype', 'N']

    def setup(self, dtype, N):
        vals = np.array([*range(55), 54, *range(55, N - 1)], dtype=dtype)
        np.random.shuffle(vals)
        indices = pd.Index(vals)
        self.data = pd.Series(np.arange(N), index=indices)

    def time_loc_slice(self, index, N):
        self.data.loc[:800]


def _select_case_params(case_type, method_name, param_indices):
    method = getattr(case_type, method_name)
    raw_params = getattr(method, "params", getattr(case_type, "params", ()))
    params = list(raw_params)
    if params and not isinstance(params[0], (tuple, list)):
        params = [params]
    else:
        params = [list(axis) for axis in params]
    return tuple(params[axis][index] for axis, index in enumerate(param_indices))


class Float64GroupIndex:
    """Aggregate hash_functions.Float64GroupIndex with frozen 920b weights."""

    case_params = (
        _select_case_params(_Official_hash_functions_Float64GroupIndex, 'time_groupby', ()),  # hash_functions.Float64GroupIndex.time_groupby
    )
    run_repeat = (
        1,  # hash_functions.Float64GroupIndex.time_groupby
    )
    case_methods = (
        'time_groupby',
    )
    case_types = (_Official_hash_functions_Float64GroupIndex,)

    def setup(self):
        lengths = {
            len(self.case_params),
            len(self.run_repeat),
            len(self.case_methods),
            len(self.case_types),
        }
        if lengths != {len(self.case_params)}:
            raise ValueError('aggregate case metadata lengths differ')
        self.cases = []
        caches = {}
        try:
            for case_type, method_name, params in zip(
                self.case_types, self.case_methods, self.case_params
            ):
                if case_type not in caches:
                    cache_owner = case_type()
                    setup_cache = getattr(cache_owner, 'setup_cache', None)
                    cache = setup_cache() if setup_cache is not None else None
                    caches[case_type] = cache
                cache = caches[case_type]
                call_params = ((cache,) if cache is not None else ()) + tuple(params)
                case = case_type()
                case_setup = getattr(case, 'setup', None)
                if case_setup is not None:
                    case_setup(*call_params)
                self.cases.append((case, method_name, call_params))
        except BaseException:
            self.teardown()
            raise

    def time_aggregate(self):
        for (case, method_name, call_params), repeat in zip(
            self.cases, self.run_repeat
        ):
            method = getattr(case, method_name)
            for _ in range(repeat):
                method(*call_params)

    def teardown(self):
        cases = getattr(self, 'cases', [])
        while cases:
            case, _method_name, call_params = cases.pop()
            case_teardown = getattr(case, 'teardown', None)
            if case_teardown is not None:
                case_teardown(*call_params)


class Unique:
    """Aggregate hash_functions.Unique with frozen 920b weights."""

    case_params = (
        _select_case_params(_Official_hash_functions_Unique, 'time_unique', (1,)),  # hash_functions.Unique.time_unique('Float64')
        _select_case_params(_Official_hash_functions_Unique, 'time_unique', (0,)),  # hash_functions.Unique.time_unique('Int64')
        _select_case_params(_Official_hash_functions_Unique, 'time_unique_with_duplicates', (1,)),  # hash_functions.Unique.time_unique_with_duplicates('Float64')
        _select_case_params(_Official_hash_functions_Unique, 'time_unique_with_duplicates', (0,)),  # hash_functions.Unique.time_unique_with_duplicates('Int64')
    )
    run_repeat = (
        1,  # hash_functions.Unique.time_unique('Float64')
        3,  # hash_functions.Unique.time_unique('Int64')
        2,  # hash_functions.Unique.time_unique_with_duplicates('Float64')
        4,  # hash_functions.Unique.time_unique_with_duplicates('Int64')
    )
    case_methods = (
        'time_unique',
        'time_unique',
        'time_unique_with_duplicates',
        'time_unique_with_duplicates',
    )
    case_types = (_Official_hash_functions_Unique, _Official_hash_functions_Unique, _Official_hash_functions_Unique, _Official_hash_functions_Unique,)

    def setup(self):
        lengths = {
            len(self.case_params),
            len(self.run_repeat),
            len(self.case_methods),
            len(self.case_types),
        }
        if lengths != {len(self.case_params)}:
            raise ValueError('aggregate case metadata lengths differ')
        self.cases = []
        caches = {}
        try:
            for case_type, method_name, params in zip(
                self.case_types, self.case_methods, self.case_params
            ):
                if case_type not in caches:
                    cache_owner = case_type()
                    setup_cache = getattr(cache_owner, 'setup_cache', None)
                    cache = setup_cache() if setup_cache is not None else None
                    caches[case_type] = cache
                cache = caches[case_type]
                call_params = ((cache,) if cache is not None else ()) + tuple(params)
                case = case_type()
                case_setup = getattr(case, 'setup', None)
                if case_setup is not None:
                    case_setup(*call_params)
                self.cases.append((case, method_name, call_params))
        except BaseException:
            self.teardown()
            raise

    def time_aggregate(self):
        for (case, method_name, call_params), repeat in zip(
            self.cases, self.run_repeat
        ):
            method = getattr(case, method_name)
            for _ in range(repeat):
                method(*call_params)

    def teardown(self):
        cases = getattr(self, 'cases', [])
        while cases:
            case, _method_name, call_params = cases.pop()
            case_teardown = getattr(case, 'teardown', None)
            if case_teardown is not None:
                case_teardown(*call_params)


class UniqueAndFactorizeArange:
    """Aggregate hash_functions.UniqueAndFactorizeArange with frozen 920b weights."""

    case_params = (
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (6,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(10)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (7,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(11)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (8,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(12)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (9,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(13)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (10,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(14)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (11,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(15)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (0,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(4)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (1,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(5)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (2,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(6)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (3,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(7)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (4,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(8)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_factorize', (5,)),  # hash_functions.UniqueAndFactorizeArange.time_factorize(9)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (6,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(10)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (7,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(11)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (8,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(12)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (9,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(13)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (10,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(14)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (11,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(15)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (0,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(4)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (1,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(5)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (2,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(6)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (3,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(7)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (4,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(8)
        _select_case_params(_Official_hash_functions_UniqueAndFactorizeArange, 'time_unique', (5,)),  # hash_functions.UniqueAndFactorizeArange.time_unique(9)
    )
    run_repeat = (
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(10)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(11)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(12)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(13)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(14)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(15)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(4)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(5)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(6)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(7)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(8)
        1,  # hash_functions.UniqueAndFactorizeArange.time_factorize(9)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(10)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(11)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(12)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(13)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(14)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(15)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(4)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(5)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(6)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(7)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(8)
        1,  # hash_functions.UniqueAndFactorizeArange.time_unique(9)
    )
    case_methods = (
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_factorize',
        'time_unique',
        'time_unique',
        'time_unique',
        'time_unique',
        'time_unique',
        'time_unique',
        'time_unique',
        'time_unique',
        'time_unique',
        'time_unique',
        'time_unique',
        'time_unique',
    )
    case_types = (_Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange, _Official_hash_functions_UniqueAndFactorizeArange,)

    def setup(self):
        lengths = {
            len(self.case_params),
            len(self.run_repeat),
            len(self.case_methods),
            len(self.case_types),
        }
        if lengths != {len(self.case_params)}:
            raise ValueError('aggregate case metadata lengths differ')
        self.cases = []
        caches = {}
        try:
            for case_type, method_name, params in zip(
                self.case_types, self.case_methods, self.case_params
            ):
                if case_type not in caches:
                    cache_owner = case_type()
                    setup_cache = getattr(cache_owner, 'setup_cache', None)
                    cache = setup_cache() if setup_cache is not None else None
                    caches[case_type] = cache
                cache = caches[case_type]
                call_params = ((cache,) if cache is not None else ()) + tuple(params)
                case = case_type()
                case_setup = getattr(case, 'setup', None)
                if case_setup is not None:
                    case_setup(*call_params)
                self.cases.append((case, method_name, call_params))
        except BaseException:
            self.teardown()
            raise

    def time_aggregate(self):
        for (case, method_name, call_params), repeat in zip(
            self.cases, self.run_repeat
        ):
            method = getattr(case, method_name)
            for _ in range(repeat):
                method(*call_params)

    def teardown(self):
        cases = getattr(self, 'cases', [])
        while cases:
            case, _method_name, call_params = cases.pop()
            case_teardown = getattr(case, 'teardown', None)
            if case_teardown is not None:
                case_teardown(*call_params)
